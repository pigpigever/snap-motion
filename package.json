{
  "name": "@snap-motion/plugin",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "run:vue": "pnpm --filter @snap-motion/vue dev",
    "build:vue": "pnpm --filter @snap-motion/vue build",
    "tag:version": "pnpm changeset version",
    "release": "lerna publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.28.0",
    "lerna": "^8.1.9"
  }
}
