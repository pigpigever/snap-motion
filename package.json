{
  "name": "snap-motion",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "run:vue": "pnpm --filter vue-snap-motion dev",
    "build:vue": "pnpm --filter vue-snap-motion build",
    "tag:version": "pnpm changeset version",
    "release": "pnpm changeset publish",
    "lerna:release": "lerna publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.28.0",
    "lerna": "^8.1.9"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pigpigever/snap-motion.git"
  },
  "keywords": [
    "snap",
    "motion",
    "vue",
    "snap-motion",
    "vue-snap-motion"
  ]
}
